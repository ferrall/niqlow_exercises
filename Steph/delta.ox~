#include "../../Documents/Github/niqlow/Steph/BLPMain.ox"

delta(theta2){
{
	decl deltanew deltaold deltafp v num den sjt;
	v = rann(20,1);
	deltanew = util;
	deltaold = zeros(nobs,1);
	deltafp = deltanew;

	while (norm(deltanew - deltaold) > 0.01)
	{
		deltaold = deltafp;
		num = zeros(nobs,1);

		for (i=0; i<(nobs+1); ++i)
		{
			for (j=1; j<21; ++j)
			{
				num[i][j] = exp(deltaold[i] + theta2[1]*v[j]*
				
			
			
		
}